# Production env — used when you run `npm run build`
# Vercel will also read this automatically.
# Replace the URL below with your actual Render backend URL once deployed.
VITE_API_URL=https://quantsignal-api.onrender.com
